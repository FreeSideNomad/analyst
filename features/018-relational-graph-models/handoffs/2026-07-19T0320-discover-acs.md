# Handoff — discover-acs (Checkpoint 2)

```yaml
checkpoint: 2
feature: 018-relational-graph-models
status: complete
human_action_needed: no   # full autonomy delegated in-session 2026-07-19; ACs approved in-handoff per owner instruction
recommended_next: /engineer.atdd to formalize as Given/When/Then specs
exit_criteria:
  - criterion: acs.md exists with frontmatter and numbered ACs
    verified_by: tool
    met: true
    evidence: "acs.md written — ac_count: 14, high_priority_count: 10"
  - criterion: all four interview passes covered
    verified_by: review
    met: true
    evidence: "happy path AC-1–9, edge AC-10–11, error AC-12, cross-cutting AC-13–14"
  - criterion: domain language enforced
    verified_by: review
    met: true
    evidence: "no endpoints/classes/status codes; graph terminology only where it IS the domain (tables, links, held-out)"
```

## Grounding corrections made during discovery

- **Reference numbers fixed**: the numbers quoted at promotion
  (0.765/0.902/0.800) are the paper's BASELINE AUROCs; the GRAPH model's
  are 0.7182/0.7592/0.6787 (RESULTS.md). feature.md corrected; the board
  validates each tier against its own reference (AC-4, AC-5).
- Berka provenance confirmed credential-free: public mirrors
  (github jlacko/berka-dataset, fallback sorry.vse.cz) listed per-table in
  the paper's `datasets/berka/schema.yaml`.
- Temporal honesty comes from the paper's task specs (predict at
  grant_date, outcome columns excluded) — surfaced as user-facing framing
  in AC-2.
- Hybrid tier has NO paper reference number (not implemented in the paper
  repo); AC-6 guards wiring correctness (no worse than a small margin
  below the stronger parent), not a supremacy claim.

## Coverage checklist

Deploy-model signals (image variant) folded into AC-10 (lean image does
not grow; honest degradation) and AC-14 (container gate on analyst:ml).
