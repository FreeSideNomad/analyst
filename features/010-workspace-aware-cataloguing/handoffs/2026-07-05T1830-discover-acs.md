---
skill: discover-acs
agent_id: main
started: 2026-07-05T1800
ended: 2026-07-05T1830
checkpoint: 2
artifacts:
  - features/010-workspace-aware-cataloguing/acs.md
exit_criteria:
  - criterion: acs.md exists with at least one AC and valid frontmatter
    verified_by: tool
    met: true
    evidence: "acs.md written with ac_count: 11, high_priority_count: 7; numbered AC-1..AC-11 sections."
  - criterion: all four discovery passes covered (happy / edge / errors+security / cross-cutting)
    verified_by: human
    met: true
    evidence: "Happy: AC-1..3, AC-11 (context cataloguing), AC-4 (retroactive), AC-6..7 (persistence). Edge: AC-5 (bounded re-cataloguing), AC-7 schema-changed branch. Errors: AC-10 (failure contained). Cross-cutting: AC-8 (governance egress bound), AC-9 (determinism)."
  - criterion: domain-coverage checklists scanned for matching surfaces
    verified_by: tool
    met: true
    evidence: "ac-coverage checklists (web-seo, auth-oauth, deploy-model) — none match 010's surface (internal cataloguing semantics; no web page, auth, or deploy change)."
  - criterion: ACs in domain language, no implementation leakage
    verified_by: judgment
    met: true
    evidence: ACs speak of tables, workspace, meaning, relationships, sessions — not modules or functions; the code-grounding lives in feature.md's problem statement.
  - criterion: human approved the AC set
    verified_by: human
    met: true
    evidence: Human reviewed the 11 ACs and the three scoping decisions (deep context, retroactive, DB persistence) and replied "looks good" (2026-07-05).
findings_summary: 11 ACs discovered and human-approved. Scoping decisions resolved with the human — deep context (linked tables' columns included), retroactive re-cataloguing (bounded), and connected-DB catalog persistence in this feature.
human_action_needed: no
human_action_kind: none
recommended_next: /engineer.atdd to formalize as Given/When/Then specs
tracker_update: local://workspace-aware-cataloguing (in-progress, checkpoint 2)
status: complete
---

# discover-acs — handoff summary

## What I did
Discovered the acceptance criteria for workspace-aware cataloguing from
`feature.md` + the human's three scoping decisions of 2026-07-05: **deep
context** (other tables' names + descriptions, columns of directly-linked
tables, the relationship graph), **retroactive** re-cataloguing of affected
tables, and **connected-DB catalog persistence** — all in scope. Wrote the 11
ACs to `acs.md`; the human approved ("looks good"). Committed `d199129`.

## Artifacts produced
- `features/010-workspace-aware-cataloguing/acs.md` (11 ACs, 7 high)

## Findings
- The determinism constraint (AC-9) is load-bearing: workspace-aware enrich
  descriptions change the planner prompt, so QA cassettes for multi-table
  workspaces will churn once and must stay stable after re-record.
- Governance (AC-8): the workspace context is metadata-only by construction —
  names/descriptions/roles/relationships, never bulk rows.

## Recommended next step
`/engineer.atdd` — formalize the 11 ACs as a standard-Gherkin `spec.md` and
generate the acceptance-test pipeline.
