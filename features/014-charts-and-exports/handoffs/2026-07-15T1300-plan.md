---
skill: plan
agent_id: main
started: 2026-07-15T1240
ended: 2026-07-15T1300
checkpoint: 4
artifacts:
  - features/014-charts-and-exports/plan.md
findings_summary: Architecture grounded in the existing answer machinery — AnswerResult already carries chart payloads + trust trail, so a saved chart persists the VALIDATED SQL + chart config and re-runs on open through the same interpretation function Q&A uses (extracted, extended with temporal line inference; one source of truth). Exports live in a new engine module — CSV/Parquet via DuckDB COPY, Excel via openpyxl write-only streaming (deliberately NOT the DuckDB excel extension: it downloads on first use, which would break AC-11's offline mandate). Charts persist as a charts.json workspace sidecar (013 pattern); stored SQL is re-guarded on every open/export. Charter check all compliant, no amendments, no runbook. Plan approved under the standing delegation.
human_action_needed: no
human_action_kind: none
recommended_next: implement (CP5) in-session under the standing delegation
tracker_update: local://charts-and-exports (planned)
exit_criteria:
  - criterion: "plan.md complete with all seven sections"
    verified_by: tool
    met: true
    evidence: "frontmatter checkpoint: 4; Architecture/Charter Check/Phasing/Performance budgets/Collaboration schedule/Execution modes/Test strategy present"
  - criterion: "Charter Check has no unresolved deviation"
    verified_by: review
    met: true
    evidence: "all rows ✅; Amendments: none"
  - criterion: "Test strategy incorporates validation_method"
    verified_by: review
    met: true
    evidence: "re-run pin via refresh round-trip, export fidelity byte-checks, browser e2e, three named mutation gates"
status: complete
---

# plan — handoff summary

The two decisions that matter: saved charts re-run (never snapshot), and
Excel through openpyxl to keep offline behavior identical. Implementer
starts at Phase 1 (chart lifecycle), unit-red first.
