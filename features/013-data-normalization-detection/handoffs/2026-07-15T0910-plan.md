---
skill: plan
agent_id: main
started: 2026-07-15T0845
ended: 2026-07-15T0910
checkpoint: 4
artifacts:
  - features/013-data-normalization-detection/plan.md
findings_summary: Architecture settled on a view-overlay design grounded in the store's actual mechanism (datasets are DuckDB views over versioned Parquet; profiling/Q&A/export all read the view) — approve rewrites the view SELECT with an explicit frozen value mapping, revoke restores the plain view, Parquet never changes. Domain gets pure policy (canonical-key + tie-break), engine gets detect() + store view-overlay methods, repository orchestrates with catalog-style sidecars (boot + refresh re-assert), API extends the dataset routes, workbench gets indicator + drilldown actions. Charter check: all rows compliant; one clarification recorded (deterministic engine-layer detection satisfies AC-12's no-model mandate; agentic layer keeps ownership of future judgment-based extensions) — no amendment needed, no deviations. No runbook (no infra surface). Plan approved under the in-session full-autonomy delegation.
human_action_needed: no
human_action_kind: none
recommended_next: implement (CP5) — proceeding in-session under the standing delegation
tracker_update: local://data-normalization-detection (planned)
exit_criteria:
  - criterion: "plan.md exists with Architecture, Charter Check, Phasing, Performance budgets, Collaboration schedule, Execution modes, Test strategy"
    verified_by: tool
    met: true
    evidence: "plan.md frontmatter checkpoint: 4, plan_status: approved; all seven sections present"
  - criterion: "Charter Check has no unresolved deviation"
    verified_by: review
    met: true
    evidence: "compliance table all ✅; Amendments: none required (one recorded clarification, no rule violated)"
  - criterion: "Test strategy incorporates the feature's validation_method"
    verified_by: review
    met: true
    evidence: "Test strategy names the messy-fixture board, the three mutation gates, browser e2e — matching feature.md validation_method verbatim in substance"
  - criterion: "Architecture grounded in actual code"
    verified_by: review
    met: true
    evidence: "plan cites engine/store.py _register_parquet view mechanism, profile_relation, 010 sidecar pattern, repository protocol as read this session"
status: complete
---

# plan — handoff summary

CP4 complete. The one non-obvious call: an approved rule freezes its value
mapping — future new variants surface as NEW proposals rather than folding
in silently, extending "never silently applied" across time. Implementer
(same session) starts at Phase 1: domain policy + engine detection,
unit-red first, then binds the six detection scenarios.
