---
skill: discover-acs
agent_id: main
started: 2026-07-01T1300
ended: 2026-07-01T1313
checkpoint: 2
artifacts:
  - features/001-file-ingestion-and-profiling/acs.md
exit_criteria:
  - criterion: acs.md exists with at least one AC and valid frontmatter
    verified_by: tool
    met: true
    evidence: "acs.md written with ac_count: 24, high_priority_count: 14; numbered AC-1..AC-24 sections."
  - criterion: all four discovery passes covered (happy / edge / errors+security / cross-cutting)
    verified_by: human
    met: true
    evidence: Interviewed one pass at a time; AC-1..8 happy, AC-9..12 edge, AC-13..17 errors+security, AC-18..24 cross-cutting. Coverage summary in acs.md.
  - criterion: domain-coverage checklists scanned for matching surfaces
    verified_by: tool
    met: true
    evidence: "ls ac-coverage → auth-oauth/deploy-model/web-seo only; none match 001's surface (auth deferred; data-migration.md not written). Recorded in acs.md."
  - criterion: ACs in domain language, no implementation leakage
    verified_by: judgment
    met: true
    evidence: Bodies describe observable behavior; storage/agent mechanics kept out of AC text (noted in acs.md header).
findings_summary: 24 ACs discovered for file ingestion & profiling. Refresh-with-schema-validation (AC-18) and low-confidence cataloguing (AC-22) make 001 exercise the AskQuestion primitive. Governance invariant (AC-16) and golden-corpus validation (AC-24) are the load-bearing quality ACs.
human_action_needed: yes
human_action_kind: review
recommended_next: Human reviews/approves ACs, then /engineer.atdd to formalize as Given/When/Then specs + generate the test pipeline.
tracker_update: none (local tracker = feature folder; checkpoint advances on progress.md, produced downstream).
status: complete
---

# discover-acs — handoff summary

## What I did
Ran Checkpoint 2 (greenfield) for feature 001. Interviewed the human through the four discovery passes (happy path, edge cases, errors & security, cross-cutting), resolving genuine open decisions at each step, then wrote 24 acceptance criteria to `acs.md` in domain language.

## Artifacts produced
- `features/001-file-ingestion-and-profiling/acs.md` (24 ACs, 14 high-priority).

## Findings
- **Autopilot with messiness recorded** is the consistent stance: mixed types, headerless files, duplicate columns, encodings — all handled automatically and recorded in the profile rather than rejected.
- **Feature 001 builds AND uses the AskQuestion primitive** — on refresh-validation failure (AC-18) and very-low-confidence cataloguing (AC-22).
- **AC-18 (refresh: reload into existing schema, validate before replace, ask to loosen on conflict)** is the meatiest sub-capability and the natural split point if 001 is too large at plan time.
- **AC-16 governance invariant** (capped samples + egress log asserting no bulk data leaves) and **AC-24 golden-corpus validation** are the load-bearing quality ACs. A golden-corpus curation research task (Kaggle + wild) is in progress and will seed the ATDD fixtures.
- Formats grew to CSV + TSV + Excel (per-sheet) + JSON; envelope ~1GB/few-million rows; delete included.

## Human action needed?
Yes — **review/approve the ACs**. This is an explicit stop point; I will not auto-dispatch CP3 (`atdd`) until you approve.

## Recommended next step
Approve the ACs → `/engineer.atdd` to formalize Given/When/Then specs and generate the project's acceptance-test pipeline. (The golden-corpus research will fold into the fixtures.)

## Tracker update
None — local tracker is the feature folder; checkpoint state advances via `progress.md` (produced downstream).
