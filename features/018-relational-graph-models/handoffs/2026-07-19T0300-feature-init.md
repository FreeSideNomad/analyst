# Handoff — feature-init (Checkpoint 1.5)

```yaml
checkpoint: 1.5
feature: 018-relational-graph-models
status: complete
human_action_needed: no
recommended_next: /engineer.discover-acs (autonomous session — full autonomy delegated in-session 2026-07-19)
```

## What was done

- Promoted roadmap item `relational-graph-backend` → `features/018-relational-graph-models/`
  (`status: ready`, `autonomy_level: high`, `roadmap_ref` back-linked, item marked in-progress).
- Branch `relational-graph-models` created from main (post-012-merge, e5a2457).
- Owner decisions locked at promotion (recorded in feature.md):
  1. torch stack in a separate `analyst:ml` image variant; container e2e gate runs against it;
  2. Berka-only reference data; board asserts GNN AUROC within ±0.03 of the paper's
     loan_default 0.765 / account_churn 0.902 / card_adoption 0.800, deterministic seeds;
  3. scope = validated engine + registry integration incl. hybrid GNN→GBM; guided
     graph-schema authoring deferred.
- Validation method: reference-data board + `analyst:ml` container e2e (012 pattern).

## Notes for discover-acs

- Source material: `~/code/relational-graph` (relbench, pytorch-frame, torch-geometric,
  CPU torch 2.8, `datasets/berka/hooks.py`, RESULTS.md reference numbers) and
  `features/012-guided-predictive-models/research/01-origin-relgraph-findings.md`.
- The honesty bar is reproduction of reference results, NOT beating the GBM baseline
  (the paper's own finding: baselines won every public-data task).
- Reuse 012 seams: gallery/on-demand cache (`ANALYST_ML_CACHE`), registry,
  predictions-as-datasets, ModelsPage, replay cassettes, container-board bindings.
