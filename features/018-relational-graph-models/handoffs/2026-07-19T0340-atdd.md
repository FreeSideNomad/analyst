# Handoff — atdd (Checkpoint 3)

```yaml
checkpoint: 3
feature: 018-relational-graph-models
status: complete
human_action_needed: no   # spec approved in-handoff per delegated autonomy
recommended_next: /engineer.plan
exit_criteria:
  - criterion: spec.md parses to a valid IR
    verified_by: tool
    met: true
    evidence: "dae_gherkin.py -> .build/spec.json; generator: 'generated 15 test(s) from 14 scenario(s)'"
  - criterion: every AC maps to >=1 scenario
    verified_by: review
    met: true
    evidence: "AC-1..14 all tagged; AC-4/5 covered twice (fast loan_default scenario + ML_FULL outline for account_churn/card_adoption)"
  - criterion: board runs red with explicit NOT YET IMPLEMENTED
    verified_by: tool
    met: true
    evidence: "pytest: 15 failed in 3.10s, failures are NOT YET IMPLEMENTED (stub acceptance/e2e_018.py registry)"
```

## Design notes carried to plan

- Fast board = loan_default end to end (paper's own artifacts suggest it
  trains in under a minute); ML_FULL=1 gates the two heavier tasks'
  reference-matrix outline (run before ship + nightly), mirroring the
  E2E=0 convention.
- Tolerance ±0.03 vs RESULTS.md, per tier per task.
- CI lesson from 012 applied: the branch will not meet CI until the PR
  opens (push trigger is main-only), so the red board never breaks GH
  Actions while slices land.
